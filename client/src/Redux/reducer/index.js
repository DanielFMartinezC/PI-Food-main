import { GET_RECIPES, GET_RECIPE_DETAIL, CREATE_RECIPE, GET_DIETS } from '../actions/index';

const initialState = {
    recipes: [],
    diets: [],
    recipeDetail: {}
};

const rootReducer = (state = initialState, action) => {

}